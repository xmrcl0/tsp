#include <stdio.h>

void print_path(float ***d, int **p, size_t n, float l, int m);
void print_dist(float ***d, size_t n);
void print_cord(float ***v, size_t n);
void print_minp(float l);
void print_repo(float **c, float **d, int *min_p, int n, float min_l, char *inter, char *mode);
